# Add paths relative to repository root that should be taken into account when generating a hash
packages:
  mpi:
    - apptainer/mpi.def
  petsc:
    - petsc
    - apptainer/petsc.def
    - conda/petsc/meta.yaml
    - scripts/configure_petsc.sh
    - scripts/update_and_rebuild_petsc.sh
  libmesh:
    - libmesh
    - apptainer/libmesh.def
    - conda/libmesh/meta.yaml
    - scripts/configure_libmesh.sh
    - scripts/update_and_rebuild_libmesh.sh
  moose:
    - .
    - conda/moose/meta.yaml
    - conda/moose/conda_build_config.yaml
# Add top level items above, in order of dependency (mpi <-- petsc <-- libmesh, etc)
zip_keys:
  - mpi
  - petsc
  - libmesh
  - moose
